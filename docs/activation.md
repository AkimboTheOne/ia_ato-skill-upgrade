# Activation

The explicit activation command is `@skill-upgrade`.

The CLI entrypoint is:

```bash
ato-skill-upgrade
```

The skill classifies natural language requests into `maturity`, `feature`, or `document` when possible.

