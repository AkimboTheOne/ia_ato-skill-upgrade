# Security

Baseline v0.2 forbids:

- reading outside the repo without approval,
- printing secrets,
- arbitrary shell execution,
- default writes,
- code writes in `document` mode,
- undocumented scope expansion.

